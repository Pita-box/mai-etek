echo "# mai-etek" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Pita-box/mai-etek.git
git push -u origin main

git remote add origin https://github.com/Pita-box/mai-etek.git
git branch -M main
git push -u origin main

git add . – připraví všechny nové a upravené soubory v aktuální složce.
git commit -m "..." – vytvoří tzv. commit (můžete si upravit text v uvozovkách podle potřeby).
git push – odešle tyto změny do vašeho repozitáře na GitHubu.